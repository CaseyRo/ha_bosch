# Daily Log — 2026-02-23

> Append-only. Raw capture. Promote important items to registers via /recall-promote.

## Session Start

- Total Recall memory system initialized for bosch custom component project.

---

## Notes

[session] Implemented POINTTAPI OAuth config flow UX improvements (plan: "Improve POINTTAPI OAuth Config Flow UX").

**Files changed:**
- `config_flow.py`: Added `SelectSelector` (radio list) for easycontrol_protocol step with labels "Local connection (XMPP)" / "Cloud / Bosch Account". Fixed reauth routing to show login URL step first (`async_step_pointtapi_oauth_open`) instead of jumping to paste step. Replaced single `oauth_callback_required` error with 3 specific keys: `oauth_callback_empty`, `oauth_callback_invalid`, `oauth_token_failed`.
- `strings.json`: Rewrote all 4 POINTTAPI step strings — better titles/descriptions, clickable markdown link for auth URL, clearer paste-URL instructions, updated field labels. Added 3 new error strings.
- `translations/en.json`: Added all 4 POINTTAPI config flow steps (were entirely absent), new error keys, `reauth_invalid` and `reauth_successful` abort keys.

**Constraint honored:** XMPP flow (`xmpp_config`, `async_step_xmpp_config`, `async_step_protocol`, `async_step_http_config`, IVT/NEFIT/MBLAN paths) untouched.

---
